const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { await prisma.users.updateMany({ data: { role: 'OWNER' } }); console.log('Updated all users to OWNER'); } main().catch(console.error).finally(() => prisma.$disconnect());
