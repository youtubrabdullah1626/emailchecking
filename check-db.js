const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const step = await prisma.sequenceStep.findFirst({ where: { status: 'SENT' }, include: { sequence: { include: { prospect: true } } }, orderBy: { sent_at: 'desc' } }); console.log(JSON.stringify(step, null, 2)); } main().catch(console.error).finally(() => prisma.$disconnect());
